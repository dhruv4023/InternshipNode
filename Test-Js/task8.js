// Create 2 strings and perform the below operations.

let x="hello world!"
let y="internship in nodejs! nodejs is a nodejs is an open-source, server-side JavaScript runtime environment"
// a. Replace
console.log(y.replace("nodejs","spring boot"))

// b. ReplaceAll
console.log((y).replaceAll("nodejs","spring boot"))

// c. Search
console.log(y.search("nodejs"))

// d. Slice
console.log(x.slice(0,5))

// e. Split
console.log(x.split(" "))

// f. Match
console.log(y.match("nodejs"))

// g. Concat
console.log(x+" "+y)